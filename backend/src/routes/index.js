const groupRoutes = require('./GroupRoute');
const notesRoutes = require('./GroupNotesRoute');

// Export the routes so they can be imported collectively elsewhere
module.exports = {
    groupRoutes,
    notesRoutes
};